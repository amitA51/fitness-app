/**
 * ============================================================================
 * STUB - Theme system removed
 * ============================================================================
 *
 * The legacy WORKOUT_THEMES preset map, THEME_PRESETS, and ThemeVariable were
 * removed: they had zero importers and hardcoded off-brand/duplicated hex that
 * drifted from tokens.css. Theming is owned by tokens.css (light + dark).
 */

export function getThemeVariables(_themeName: string): Record<string, string> {
  return {};
}
