## [16] HIGH — WorkoutPlanScreen renders raw English muscle-group names to Hebrew users
**File:** src/components/workout/states/WorkoutPlanScreen.tsx (line ~321)
**Dimension:** hebrew-copy
**Description:** The per-exercise subtitle prints `{ex.muscleGroup}` directly. muscleGroup values are stored in English in the data layer ('Chest', 'Back', 'Cardio', etc. — see src/data/builtInExercises.ts lines 46/20 and the `muscleGroup?: string` type in src/types/index.ts). So on the pre-workout planning table every exercise card shows an English muscle label (CHEST, BACK, CARDIO) inside an otherwise fully-Hebrew RTL screen. Other screens (ForecastNudge, ExerciseFilter, PreWorkoutScreen) translate these via a MUSCLE_HE map; this screen forgot to.
**Proposed fix:** Map ex.muscleGroup through a shared Hebrew muscle-group dictionary before rendering (extract the duplicated MUSCLE_HE maps into one shared util, e.g. src/utils/muscleGroups.ts, and reuse it here).
**Verifier evidence:** Verified against source. WorkoutPlanScreen.tsx:321 renders {ex.muscleGroup} raw (with textTransform: uppercase), and the screen is otherwise fully Hebrew RTL. The data layer stores English muscle groups: src/data/builtInExercises.ts has muscleGroup: 'Cardio' (line 20/32) and 'Chest' (line 46), and src/types/index.ts:43 types it as muscleGroup?: string. The value flows untranslated from personalExercise.muscleGroup through ExerciseSelector/index.tsx (lines 88/114/136) and PlanEditorModal.tsx into the plan exercises, so for built-in exercises the user sees English labels like CHEST/BACK/CARDIO. The three sibling screens named in the finding all translate via a Hebrew map and were confirmed: ForecastNudge.tsx:11 MUSCLE_HE {Chest:'חזה'...}, ExerciseFilter.tsx:12 MUSCLE_LABELS {Chest:'חזה'...,Cardio:'אירובי'}, PreWorkoutScreen.tsx:62 MUSCLE_SUGGESTIONS {Chest:'חזה'...}. WorkoutPlanScreen has no such map (grep found only the two raw {ex.muscleGroup} usages). This violates the project's ui-preflight.md rule requiring all visible Hebrew strings be correct. One caveat: a user-authored custom exercise (QuickExerciseForm free-text muscleGroup) could already be Hebrew and render fine, but every seeded/built-in exercise is English, which is the default case. Severity HIGH is somewhat generous since this is a cosmetic copy issue rather than a functional break, but the finding is genuine and worth fixing, and the proposed shared-util fix is sound (the map is duplicated across 3+ files, so extraction also satisfies DRY).

## [17] MEDIUM — User-facing micro-labels left in English (STREAK / BEST) against Hebrew-first spec
**File:** src/components/dashboard/WorkoutStreak.tsx (line ~96)
**Dimension:** hebrew-copy
**Description:** The streak card on the dashboard shows the literal English words `STREAK` (line 96) and `BEST:` (line 100) to Hebrew users. DESIGN.md line 90 explicitly says mono micro-labels are Hebrew uppercase kickers (example given: 'תכנון מראש'), so these are not intentional Latin branding. The aria-label is correctly Hebrew ('רצף אימונים'), making the visible English text an inconsistency with the component's own accessible name.
**Proposed fix:** Replace with Hebrew kickers, e.g. 'רצף' for STREAK and 'שיא:' for BEST:.
**Verifier evidence:** Verified in C:\Users\amit0\desktop\fitness-app\src\components\dashboard\WorkoutStreak.tsx. Line 96 renders the literal English word `STREAK` and lines 99-100 render `BEST:` inside mono uppercase kicker spans (fontFamily var(--font-mono), letterSpacing, textTransform uppercase). The component's own aria-label (line 70) is correctly Hebrew (`רצף אימונים: ${n} ימים`), so the visible English text contradicts the accessible name. DESIGN.md line 90 states verbatim: "Hebrew-first... Mono micro-labels are uppercase kickers ('תכנון מראש')" — the example is Hebrew, confirming kickers are meant to be Hebrew, not Latin branding. Convention check confirms this: the identical mono-kicker styling in the sibling WeeklyGrid.tsx (line 140) renders the Hebrew word `היום` ("today"), not "TODAY", and a repo-wide grep across src/ found `STREAK` as the ONLY English mono micro-label — these two are outliers. No code comment, design note, or rationale anywhere treats STREAK/BEST as intentional Latin branding. The finding is accurate and worth fixing (proposed Hebrew replacements like רצף / שיא: are reasonable). Minor caveat: MEDIUM severity is slightly generous for two cosmetic words, but it is user-facing copy that directly violates a written spec, so the finding stands.

## [18] MEDIUM — Hebrew muscle terminology is wrong and misspelled for Biceps/Triceps
**File:** src/components/workout/states/PreWorkoutScreen.tsx (line ~66)
**Dimension:** hebrew-copy
**Description:** MUSCLE_SUGGESTIONS maps Biceps→'זרועות קידמיות' and Triceps→'זרועות אחוריות' (lines 66-67). This is incorrect Hebrew: biceps/triceps are 'יד קדמית'/'יד אחורית' or simply 'דו-ראשי'/'תלת-ראשי', not 'front/back arms (plural)'. Also 'קידמיות' is a misspelling of 'קדמיות' (extra yod), repeated for Quadriceps→'ירכיים קידמיות' (line 68). These strings surface in the pre-workout suggestion card ('מזמן לא אימנת {hebrewName}').
**Proposed fix:** Correct to standard gym Hebrew and fix spelling: Biceps→'יד קדמית' (or 'דו-ראשי'), Triceps→'יד אחורית' (or 'תלת-ראשי'), Quadriceps→'ארבע-ראשי' (or 'ירך קדמית'), Hamstrings→'ירך אחורית'. Fix 'קידמיות'→'קדמיות'.
**Verifier evidence:** VERIFIED REAL in src/components/workout/states/PreWorkoutScreen.tsx (MUSCLE_SUGGESTIONS, lines 62-74). Two defects confirmed against the codebase's own conventions:

1) SPELLING: Lines 66 and 68 use 'קידמיות' (extra yod). Every other file spells it correctly without the yod: builtInWorkoutTemplates.ts L83/L192 ('יד קדמית'), builtInExercises.ts L233/L269/L451 ('יד קדמית'/'כתף קדמית'), WarmupCooldownFlow.tsx L51 ('ירך קדמית'). So 'קידמיות' is a genuine misspelling, inconsistent with the project SSOT.

2) TERMINOLOGY: Biceps→'זרועות קידמיות' and Triceps→'זרועות אחוריות' (plural 'front/back arms') is non-standard. The codebase's established muscle-group labels are 'יד קדמית'/'יד אחורית' (builtInWorkoutTemplates.ts L83/L192, builtInExercises.ts L903). Quadriceps→'ירכיים קידמיות' also diverges from the codebase standard 'ארבע-ראשי' (10+ uses in builtInExercises.ts).

USER-FACING: These strings render in the pre-workout suggestion card — `מזמן לא אימנת ${hebrewName}` (L179) and `אימנת ${hebrewName} לאחרונה` (L203) via MUSCLE_SUGGESTIONS[muscle] lookups (L176/L188/L200).

This matches the project's ui-preflight.md rule requiring grammatically correct Hebrew. Worth fixing. Minor caveat: the proposed Hamstrings change ('ירכיים אחוריות'→'ירך אחורית') is optional/stylistic — current Hamstrings value has no misspelling; the load-bearing fixes are biceps/triceps terminology and the 'קידמיות'→'קדמיות' spelling on L66 and L68.

## [19] MEDIUM — 'Core' translated inconsistently across screens (בטן vs ליבה)
**File:** src/components/dashboard/ForecastNudge.tsx (line ~17)
**Dimension:** hebrew-copy
**Description:** ForecastNudge maps `Core: 'בטן'` (line 17) and has no 'Abs' key, while PreWorkoutScreen maps `Abs: 'בטן'` (line 72) AND `Core: 'ליבה'` (line 73). So the same muscle concept 'Core' renders as 'בטן' on the dashboard nudge but 'ליבה' on the pre-workout screen, and 'בטן' now denotes two different source groups depending on the file. Each screen redefines its own ad-hoc muscle dictionary, guaranteeing drift.
**Proposed fix:** Create one canonical Hebrew muscle-group map in a shared util and import it everywhere (ForecastNudge, PreWorkoutScreen, ExerciseFilter all duplicate this). Decide Core→'ליבה' and Abs→'בטן' once.
**Verifier evidence:** Confirmed by reading the three files. ForecastNudge.tsx line 17 maps Core:'בטן' (no Abs key). PreWorkoutScreen.tsx (actual path src/components/workout/states/PreWorkoutScreen.tsx, lines 72-73) maps Abs:'בטן' and Core:'ליבה'. A third dictionary in ExerciseFilter.tsx (src/components/workout/components/ExerciseFilter.tsx, lines 19,21) maps Core:'גוף' and Abs:'בטן'. So Core renders as three different Hebrew words across screens (בטן / ליבה / גוף), and בטן denotes Core in one file but Abs in two others — the drift is actually worse than the finding states. MUSCLE_GROUPS in constants/workoutConstants.ts (lines 37,39) defines Core and Abs as distinct real source groups, and analyticsService.getMuscleKey passes the raw muscleGroup string straight through, so Core is a reachable key. Each screen redefines its own ad-hoc map with no canonical source, which violates the project's DRY rule in .claude/rules/common/coding-style.md. One caveat: ForecastNudge only emits a muscle label for MAJOR_MUSCLES (Chest/Back/Legs), so its specific Core:'בטן' entry is currently unreachable in the overdue branch — but the entry is still wrong, and the Core drift between PreWorkoutScreen (ליבה) and ExerciseFilter (גוף) is fully user-visible. The proposed fix (single shared canonical Hebrew muscle map, Core->ליבה, Abs->בטן) is correct. MEDIUM severity is appropriate.

## [20] MEDIUM — Plural-agreement bug: '1 תרגילים' / '1 סטים' for singular counts
**File:** src/components/workout/states/WorkoutPlanScreen.tsx (line ~228)
**Dimension:** hebrew-copy
**Description:** Counts are concatenated with a hardcoded plural noun: `{draft.length} תרגילים · {totalSets} סטים` (line 228). When a value is 1 this renders ungrammatical Hebrew ('1 תרגילים' instead of 'תרגיל אחד'/'תרגיל 1'). The same uncorrected pattern appears in many places — WorkoutSummary.tsx:515, PreWorkoutScreen.tsx:473/710, WorkoutHistory.tsx:349, ClientDetail.tsx:164, EmbeddedTemplatePicker.tsx:102, WarmupCooldownFlow.tsx:227. Notably WorkoutStreak.tsx:94 DOES handle singular ('יום' vs 'ימים'), proving the codebase knows the rule but applies it inconsistently.
**Proposed fix:** Add a small pluralize helper (e.g. n===1 ? 'תרגיל' : 'תרגילים', 'סט'/'סטים', 'שבוע'/'שבועות') and use it at all count sites, mirroring the existing day-pluralization in WorkoutStreak.
**Verifier evidence:** VERIFIED REAL. Line 228 of src/components/workout/states/WorkoutPlanScreen.tsx is exactly as quoted: `{draft.length} תרגילים · {totalSets} סטים — ...`. Both counts are user-driven and can equal 1: `draft` is a user-built exercise list (one exercise is a normal case), `totalSets = draft.reduce(... ex.sets.length)` can be 1, and the surrounding empty-state guard only catches length 0, so the header renders "1 תרגילים · 1 סטים" for a one-exercise/one-set plan. That is genuinely ungrammatical Hebrew: for cardinal 1 the noun stays singular ("תרגיל אחד"/"סט אחד"), unlike English where "1 items" is merely informal. The inconsistency claim is confirmed — the codebase already handles singular elsewhere: src/components/dashboard/WorkoutStreak.tsx:94 (`streak.current === 1 ? 'יום' : 'ימים'`) and PreWorkoutScreen.tsx:177 (`muscle.daysSince === 1 ? 'יום' : ...ימים`). The cited duplicate sites WorkoutSummary.tsx:515, PreWorkoutScreen.tsx:473/710, ClientDetail.tsx:164, WarmupCooldownFlow.tsx:227 all reproduce the uncorrected `{n} תרגילים`/`{n} סטים` pattern. The fix is also aligned with the project's own .claude/rules/common/ui-preflight.md Hebrew copy self-audit requiring grammatically correct Hebrew. Minor finding inaccuracies that don't change the verdict: WorkoutStreak.tsx actually lives under components/dashboard/ (not near the workout states folder); WorkoutHistory.tsx:349 and EmbeddedTemplatePicker.tsx:102 were not individually re-confirmed but the core bug and most cited sites are verified. The proposed pluralize-helper fix mirroring the existing day-pluralization is sound and DRY. Severity MEDIUM is appropriate (visible copy-correctness polish, not a functional break).

## [24] HIGH — Systemic dark-mode contrast failure: --fs-heading text/icons on --fs-accent fills
**File:** src/components/workout/components/WorkoutHeader.tsx (line ~313)
**Dimension:** a11y
**Description:** Many interactive surfaces set `background: 'var(--fs-accent)'` with `color: 'var(--fs-heading)'`. In dark mode tokens.css resolves `--fs-heading: var(--fs-ink)` = #f0f0f0 (near-white) and `--fs-accent` = #4ddcbb (bright mint) → contrast ≈ 1.4:1, far below WCAG AA 4.5:1 (3:1 for large/icon). tokens.css line 81-83 explicitly warns: "NEVER on accent/primary fills — in dark it resolves near-white and fails on --fs-accent. Use --color-ink-on-*." This exact pairing appears in 13 files including WorkoutHeader.tsx (icon button, line 313/315), ExerciseReorder.tsx 'שמור' button (line 367), ExerciseReorderItem.tsx superset badge (line 225), ExerciseSelector, WorkoutSummary, PlanSetRow, WorkoutCalendar, ProfileAvatar, IconBox, ExerciseFilter, ForecastChart, SetEditRow.
**Proposed fix:** Replace `color: 'var(--fs-heading)'` with `color: 'var(--color-ink-on-accent)'` (defined #071412 in both themes) anywhere the background is `var(--fs-accent)`. Add a Biome/grep guard for the `--fs-accent` background + `--fs-heading` color co-occurrence to prevent regressions.
**Verifier evidence:** CONFIRMED REAL and worth fixing. The headline instance is verified: src/components/workout/components/WorkoutHeader.tsx lines 313/315 sets background:'var(--fs-accent)' with color:'var(--fs-heading)' on the finish-workout icon button. Token resolution confirmed in src/styles/tokens.css: dark mode (line 441) --fs-heading resolves to var(--fs-ink) = #f0f0f0 (line 364, near-white) and --fs-accent = #4ddcbb (line 368, bright mint) -> contrast ~1.4:1, far below WCAG AA (4.5:1 text / 3:1 large/icon). tokens.css lines 81-82 EXPLICITLY warn against this exact pairing ("NEVER on accent/primary fills ... Use --color-ink-on-*"), and --color-ink-on-accent=#071412 is defined in both themes (lines 85/443). The proposed fix matches the project's own documented convention and its prior "fs-ink-on-fs-primary bug class" remediation history.

CAVEAT on scope: the claimed "13 files" is overstated. My same-element verification found 9 GENUINE instances of the dangerous accent-bg + heading-color pairing: WorkoutHeader.tsx (313/315), ExerciseReorder.tsx (366/367, 'שמור' button), ExerciseReorderItem.tsx (225/226, superset badge), ProfileAvatar.tsx (16), ExerciseFilter.tsx (284/285, FAV button), PlanSetRow.tsx (108/110), ExerciseSelector/index.tsx (393/394), WorkoutSummary.tsx (772/773), SetEditRow.tsx (293/302, 'שמור' button). Four of the listed files are FALSE POSITIVES where the proximity match spanned separate elements or the code is already correct: IconBox.tsx (accent bg correctly pairs with --fs-primary; --fs-heading is on a neutral --fs-surface-2 branch), WorkoutCalendar.tsx and ForecastChart.tsx (accent is on a small self-closing divider element, --fs-heading is on a separate <h3> with no accent bg), and ExerciseDisplay.tsx (the accent element already correctly uses --color-ink-on-accent). So the severity HIGH and the per-line fix are valid for the 9 real sites, but a blanket replacement guided only by file-level co-occurrence would wrongly touch the 4 false-positive files. A regression guard must match same-element pairings, not mere co-occurrence, to avoid these false positives.

## [32] HIGH — Muscle-group key resolution drifts between `targetMuscle||muscleGroup` and `muscleGroup||targetMuscle`
**File:** src/components/insights/MuscleDistribution.tsx (line ~35)
**Dimension:** architecture
**Description:** The order of the muscle fallback is inconsistent across muscle-aggregation call sites. MuscleDistribution.tsx:35 uses `ex.targetMuscle || ex.muscleGroup`, while analyticsService.getMuscleKey:166, insightsAggregator.ts:138 and :215, WorkoutActions.tsx:123/235, and WorkoutSummaryView.tsx:49 use the opposite `muscleGroup || targetMuscle`. When an exercise carries BOTH fields with different values (common after editing/importing), the same exercise is bucketed under different muscles on different screens, so the muscle-distribution chart, insights, and summary disagree with each other. There is no shared helper — getMuscleKey exists in analyticsService but is private and not reused.
**Proposed fix:** Promote a single getMuscleKey(exercise) to utils/workoutMath.ts (or a muscleUtils module) with ONE documented priority, and call it from all six+ sites. Pick the priority deliberately (muscleGroup is the canonical taxonomy field) and delete the local variants.
**Verifier evidence:** VERIFIED REAL. MuscleDistribution.tsx:35 uses `ex.targetMuscle || ex.muscleGroup`, the reverse priority of every other muscle-aggregation site: analyticsService.ts:166 getMuscleKey (`muscleGroup||targetMuscle`), insightsAggregator.ts:138 & :215, contextBuilder.ts:256/265, trainingLoadService.ts:93, analyticsService.ts:751, plus the template-write sites WorkoutSummaryView.tsx:49 and WorkoutActions.tsx:123 (`muscleGroup||targetMuscle`). The premise that both fields can diverge is confirmed: all creation paths set them equal (builtInWorkoutTemplates.ts:317/325, QuickExerciseForm.tsx:42/59, ExerciseLibraryTab.tsx:47/55, PlanEditorModal.tsx:37/38), BUT workoutReducer.ts UPDATE_EXERCISE_META (lines 214) writes only `muscleGroup` and never `targetMuscle`, so editing an exercise's muscle group mid-workout makes the two fields disagree. After that, MuscleDistribution buckets by the stale targetMuscle while insights/summary/analytics bucket by the updated muscleGroup, so the chart disagrees with other screens for the same exercise. getMuscleKey is genuinely private — codegraph shows its only 4 callers are all inside analyticsService.ts; it is not exported or reused, and the proposed shared-helper fix is sound. Severity is overstated though: divergence requires a mid-workout meta-edit (edge case, not the default where both fields are equal) and the impact is a misplaced chart row, not data loss — closer to MEDIUM than the claimed HIGH. Worth fixing.

