## [13] HIGH — Coach data screens never render the error state from useAsyncData — fetch failures silently show 'empty'
**File:** src/pages/MyCoach.tsx (line ~43)
**Dimension:** ui-mechanical
**Description:** useAsyncData (src/pages/coach/_shared.tsx:267) returns { data, loading, error, reload }, where error is set on a rejected fetch. But MyCoach destructures only `{ data: coaches, loading: coachesLoading, reload }` (lines 43-47) and `{ data: assignments, loading: aLoading, reloadAssignments }` (48-52), never reading `error`. When listMyCoaches()/listMyAssignments() rejects (offline, server 500), `loading` flips to false and `data` stays the empty initial `[]`, so the screen renders the EmptyState 'עדיין לא התחברת למאמן' (lines 119-124) — indistinguishable from a genuinely empty roster, with no error message and no retry path. The same dead-error-branch pattern exists in src/pages/coach/ClientDetail.tsx (AssignmentsBox:349, NotesBox:478 — `.error` is never referenced in the file) and src/pages/coach/CoachInvites.tsx:31 (`const { data: invites, loading, reload } = useAsyncData(...)`). This violates the ui-preflight rule that every data surface must ship a distinct error state with a retry path.
**Proposed fix:** Destructure `error` from each useAsyncData call and render an explicit error branch before the empty branch, e.g. `if (error) return <ErrorState message='לא ניתן לטעון את הנתונים' onRetry={reload} />;`. The hook and `reload` already exist; only the consuming JSX in MyCoach, ClientDetail (Assignments/Notes/main), and CoachInvites needs the error branch wired up.
**Verifier evidence:** Verified against the source. useAsyncData (src/pages/coach/_shared.tsx:267-299) returns {data, loading, error, reload} and genuinely sets `error` in its .catch (lines 287-289) on a rejected fetch. The three coach surfaces drop that error on the floor: MyCoach.tsx lines 43-52 destructure only {data, loading, reload} for both listMyCoaches and listMyAssignments, so a rejected fetch leaves loading=false and data=[] and renders the empty states at lines 119-124 ('עדיין לא התחברת למאמן') and 178-183 — indistinguishable from a genuinely empty roster, no error message, no retry. CoachInvites.tsx:31 (`const { data: invites, loading, reload }`) has the same gap (empty state lines 107-112). In ClientDetail.tsx, AssignmentsBox (350-354) and NotesBox (479) likewise never read the load `error`. This is NOT intentional/handled-elsewhere: the app has an established convention for exactly this — Templates.tsx:43 does `if (error) return <ErrorState onRetry={loadTemplates} />;`, and a reusable ErrorState component already exists at src/pages/templates/components/TemplateStates.tsx:39. The project ui-preflight rule also mandates a distinct error state with a retry path on every data surface. So the finding is real and worth fixing, and the fix is low-effort. Two corrections to the finding for accuracy: (1) the existing ErrorState takes only `{ onRetry }`, not a `message` prop, so the proposed `<ErrorState message=...>` signature is slightly off; (2) severity is closer to MEDIUM than HIGH — these are coach/secondary screens (not the core daily flow), the data still loads correctly in the happy path, and the regression only manifests on a hard fetch rejection (offline/500), degrading gracefully to an empty state rather than crashing. Real bug, correct root cause, minor inaccuracy in the proposed JSX and an arguably inflated severity label.

## [14] MEDIUM — Weekly check-in and coach forms use placeholder as the only label (no visible label above the input)
**File:** src/pages/MyCoach.tsx (line ~240)
**Dimension:** ui-mechanical
**Description:** The shared Input component (src/components/ui/Input.tsx:6) supports a visible `label` prop (rendered as a mono eyebrow above the field, lines 62-76), but these data-entry forms pass only `placeholder` + `aria-label` and no `label`. In MyCoach the weekly check-in weight field is `<Input ... placeholder='משקל (ק"ג)' aria-label='משקל' unit='ק"ג'>` (lines 240-248) and the notes Textarea `placeholder='איך עבר השבוע?'` (276-282); the invite-code field is `placeholder='קוד הזמנה'` (104). Per ui-preflight.md, placeholder-as-label is a bug because the label disappears the moment the user types — they lose track of what the field is. Same pattern in src/pages/coach/ClientDetail.tsx (recommendation Textarea:451, calorie-target Input:466, private-note Textarea:503), src/pages/coach/ProgramBuilder.tsx (program name:208, exercise name:252), and src/pages/coach/CoachGroups.tsx:47.
**Proposed fix:** Pass the visible `label` prop the Input already supports (e.g. `<Input label='משקל' unit='ק"ג' ... />`) instead of relying on placeholder text; keep the placeholder only as an example/hint. Apply across MyCoach, ClientDetail, ProgramBuilder, and CoachGroups form inputs.
**Verifier evidence:** VERIFIED REAL. Code claims are accurate: the shared Input (src/components/ui/Input.tsx lines 62-76) and Textarea (src/components/ui/Textarea.tsx lines 40-54) render a visible mono eyebrow `label` when a `label` prop is passed, but the coach-area data-entry forms pass only `placeholder` + `aria-label` and omit `label`. Confirmed instances: MyCoach.tsx weight field (240-248, placeholder='משקל (ק"ג)'), notes Textarea (276-282), invite-code (104); ClientDetail.tsx recommendation Textarea (451), calorie-target Input (466), private-note Textarea (503); ProgramBuilder.tsx program name (208) and exercise name (252); CoachGroups.tsx (47). This directly violates a project-checked-in rule: .claude/rules/common/ui-preflight.md > Forms states "Label ABOVE input, always visible. NEVER placeholder-as-label (it disappears on type)." It is NOT intentional convention — the auth flow (SignUpStep/SignInStep/ForgotPasswordStep use AnnualInput with visible label='שם מלא'/'דוא"ל'/'סיסמה') and onboarding (MobileInput wrapper forwards a label prop to Input) both use visible labels, so the coach pages are a genuine inconsistency. Caveats: every field already carries an aria-label, so this is a sighted-user UX issue (recall after typing), not a screen-reader a11y gap; and the invite-code field (104) is the weakest case (single-shot code entry, low recall risk). The proposed fix (pass the existing `label` prop, keep placeholder as hint) is correct and low-risk. WORTH FIXING for the genuine data-entry fields (weight, calories, notes), MEDIUM severity is appropriate.

## [15] MEDIUM — AddWeightModal weight field has no visible label — only a placeholder and aria-label
**File:** src/pages/progress/modals/AddWeightModal.tsx (line ~56)
**Dimension:** ui-mechanical
**Description:** The body-weight entry is a raw `<input type='number' placeholder='0.0' aria-label='משקל בק״ג'>` (lines 56-76) with no visible text label above it; the only on-screen affordance is the '0.0' placeholder, which vanishes on first keystroke, and a 'ק״ג' unit label below the field (86). The sibling AddMeasurementModal.tsx does this correctly — every input there has a real `<label htmlFor=...>` rendered above it (lines 90-102) — proving the codebase pattern exists and AddWeightModal diverges from it. ui-preflight.md: 'Label ABOVE input, always visible. NEVER placeholder-as-label.'
**Proposed fix:** Add a visible label above the weight input (e.g. a `<label htmlFor>` reading 'משקל (ק״ג)') matching the AddMeasurementModal pattern, and keep '0.0' purely as a placeholder hint.
**Verifier evidence:** Verified against the actual files. In AddWeightModal.tsx the weight field (lines 56-76) is a raw `<input type="number" placeholder="0.0" aria-label="משקל בק״ג">` with NO visible `<label>`; the only on-screen affordances are the "0.0" placeholder (which disappears on first keystroke) and a "ק״ג" unit caption rendered BELOW the input (line 86), not a label above it. The sibling AddMeasurementModal.tsx (same folder) does it correctly — each input has a real `<label htmlFor={...}>` rendered above it (lines 90-102, using useId), proving the project pattern exists and AddWeightModal diverges from it. This violates the project's own documented standard in .claude/rules/common/ui-preflight.md: "Label ABOVE input, always visible. NEVER placeholder-as-label (it disappears on type)." Both the third sibling (AddRecoveryModal) and the notes field in AddWeightModal use SliderInput/visible captions or only carry an aria-label on an optional notes field, so the primary weight input is the clear gap. The proposed fix (add a visible `<label htmlFor>` such as "משקל (ק״ג)" matching the AddMeasurementModal pattern, keep "0.0" as placeholder) is correct and low-risk. Caveat: severity is on the lower side of MEDIUM — the sheet title "עדכון משקל" plus the persistent "ק״ג" caption and the present aria-label give meaningful context, so it is not a hard a11y failure — but it is a genuine, real violation of an explicit project rule and an inconsistency with a same-folder sibling, so it is worth fixing.

