## [7] HIGH — PageThemeContext injects 6 non-brand accent colors, breaking the two-accent system
**File:** src/contexts/PageThemeContext.tsx (line ~46)
**Dimension:** design-tokens
**Description:** PAGE_THEMES hardcodes a per-route 'jewel-tone' palette and pushes it into global CSS vars (--accent-current, --dynamic-accent-start, --accent-gradient-from/to) on every navigation. This contradicts DESIGN.md's explicit two-accent system (mint action + lime signal) and the anti-slop rule 'don't let a third accent creep in'. Five entirely off-brand accents are introduced: workout steel blue '#4F8DF5', nutrition leaf green '#56C271', history slate '#7E91B5', progress violet '#8A75E8', templates amber '#E2A23F' — plus hardcoded mint '#43C7A5'/'#2C7F91' duplicating --fs-accent/--fs-accent-2. These values are also dark-mode-blind: they are the same raw hex in light and dark, so e.g. the violet/amber never shift for Obsidian. Only the 'settings' entry correctly uses var(--fs-primary)/var(--color-primary-*). Because these vars feed AnimatedNumber, glow shadows and gradients app-wide, the violation is broadly visible.
**Proposed fix:** Collapse PAGE_THEMES to tokenized values: set every route's primary to var(--fs-accent) (or a deliberate, tokenized second accent declared in tokens.css) and reference var(--color-primary-hover)/var(--color-primary-glow) like the 'settings' entry already does. If per-route identity is genuinely wanted, declare a small set of brand-approved accent tokens in tokens.css with both light and dark values and reference them by var() — never raw hex in the TS map.
**Verifier evidence:** VERIFIED REAL. src/contexts/PageThemeContext.tsx lines 46-95 hardcode raw per-route hex accents: workout #4F8DF5, nutrition #56C271, history #7E91B5, progress #8A75E8, templates #E2A23F, plus dashboard #43C7A5/#2C7F91 (duplicating --fs-accent/--fs-accent-2). Only the `settings` entry tokenizes correctly via var(--fs-primary)/var(--color-primary-*). These are pushed onto document.documentElement as --accent-current, --dynamic-accent-*, and --accent-gradient-* on every navigation (useEffect, lines 118-147).

This directly contradicts the project's own SSOT: DESIGN.md line 17 declares "A two-accent system (mint action + lime signal)"; line 94 says "never hardcode #43c7a5/#4ddcbb; use var(--fs-accent)"; the anti-slop rule (.claude/rules/common/design-aesthetics.md) says "don't let a third accent creep in" and treats hardcoded brand hex as a bug. Strongest corroboration: src/styles/tokens.css lines 106-113 deliberately define --accent-workout/-nutrition/-history/-progress/-templates AND the --accent-current default ALL as var(--fs-accent) — i.e. the token layer intentionally collapses every per-page accent to brand mint, and this runtime context overrides that SSOT with off-brand values. Dark-mode-blindness is also confirmed: the 6 hardcoded entries are identical raw hex in light and dark (no html.dark variant), so violet/amber/slate never shift for Obsidian, while only the tokenized `settings` entry adapts.

One caveat that softens (but does not invalidate) the HIGH/"broadly visible app-wide" framing: of the vars this file sets, only --accent-current is actually consumed downstream (wired via tailwind.config.js line 36 -> accent.current utility, with tokens.css line 113 default). The --dynamic-accent-* and --accent-gradient-* vars are set but referenced nowhere in CSS or TS/TSX, and I found no component using bg-accent-current/text-accent-current. So the real-world blast radius is narrower than the description's "feed AnimatedNumber, glow shadows and gradients app-wide." Still, --accent-current is a live token whose brand value is polluted on every route change, and the raw off-brand hex are bona fide token-discipline violations. The proposed fix (collapse PAGE_THEMES to var(--fs-accent)/var(--color-primary-*) like the settings entry, or declare brand-approved per-route tokens with light+dark values in tokens.css) is correct and aligns with the already-existing tokens.css intent. Worth fixing.

## [8] HIGH — IntensityMeter hardcodes a 5-color Apple rainbow palette + white text that breaks light mode
**File:** src/components/workout/components/IntensityMeter.tsx (line ~47)
**Dimension:** design-tokens
**Description:** The ZONES map hardcodes Apple-Fitness rainbow hex for zone colors and gradients: warmup '#30D158', fatburn '#FFD60A', cardio '#FF9F0A', peak '#FF453A', max '#BF5AF2' (plus '#34C759' in gradients). None of these are brand tokens and none adapt to dark mode. Compounding it, the whole component assumes a dark backdrop: text is hardcoded as text-white, text-white/60, text-white/40, bg-black/40, border-white/10 (lines ~270-480). DESIGN.md mandates 'Both modes... read correctly in light (Fresh Steel) and dark (Obsidian)'; in light mode (Fresh Steel #eef3f1 bg) white-on-white labels and the dark backdrop assumptions render text invisible/illegible.
**Proposed fix:** Replace hardcoded zone hex with a tokenized intensity scale (e.g. derive from --fs-accent / --fs-signal / --fs-warn, or add explicit --intensity-* tokens with light+dark values in tokens.css). Replace text-white/* and bg-black/40 with semantic tokens (var(--color-ink-on-dark) only when the surface is truly dark, otherwise var(--color-text)/var(--color-text-secondary)) so the meter is legible in both themes.
**Verifier evidence:** CONFIRMED in src/components/workout/components/IntensityMeter.tsx. The ZONES map (lines 47-82) hardcodes Apple-Fitness rainbow hex (#30D158, #FFD60A, #FF9F0A, #FF453A, #BF5AF2, plus #34C759/#FF6B35/#FF2D55 in gradients) — none are --fs-* tokens, none adapt to dark mode. The component also assumes a dark backdrop throughout: text-white/60, text-white, text-white/40 (lines 271,377-378,396,441,461-479), bg-black/40 (366), bg-white/10 (275), border-white/10 (452,459,467,474), and stroke rgba(255,255,255,0.1) (130). This contradicts an explicit, written project rule: DESIGN.md names tokens.css the single source of truth for light+dark and design-aesthetics.md bans hardcoded brand hex and mandates both modes read correctly; in Fresh Steel light mode the white labels would be illegible. So it is a real token-discipline violation, not a false positive or intentional convention. The proposed fix (tokenized intensity scale + semantic ink tokens) is sound. IMPORTANT CAVEATS that lower the claimed HIGH severity to ~MEDIUM: (1) the component is dead code — codegraph reports zero callers and there is no <IntensityMeter JSX anywhere in src; it is only re-exported from the barrel index.ts. The grep hits in useWorkoutSettings.ts/SettingsContext.tsx/types/index.ts are an unrelated 'intensityMeter' settings flag, not this component. So the invisible-in-light-mode defect does not currently ship. (2) The component's premium-card container class is defined in no CSS file, so the dark-backdrop assumption is currently inert. The finding is genuine and worth fixing if the component is revived, but the user-facing impact is presently nil.

## [9] HIGH — PerformanceAnalytics hardcodes rainbow stat colors and white-on-dark text
**File:** src/components/workout/components/PerformanceAnalytics.tsx (line ~421)
**Dimension:** design-tokens
**Description:** StatCard colors are passed raw hex that bypass tokens and dark mode: '#FF9F0A' (זמן אימון), '#30D158' (סטים), '#BF5AF2' (RPE), plus a '#30D158' progress color and a 'linear-gradient(90deg, #30D158, #34C759)' fill (lines 273, 421-432, 468). Only 'נפח כולל' correctly uses color="var(--fs-accent)". Like IntensityMeter, the card body uses text-white/* and border-white/10 (line ~438) which fails on the Fresh Steel light background. This is the same off-brand Apple palette duplicated, violating the two-accent rule and DESIGN.md's both-modes requirement.
**Proposed fix:** Route stat colors through tokens (var(--fs-accent), var(--color-success), var(--fs-warn), var(--fs-signal)) instead of raw hex, and replace text-white/border-white utilities with var(--color-text)/var(--color-text-secondary)/var(--color-border) so the card works in light and dark.
**Verifier evidence:** VERIFIED REAL but severity is overstated (effectively LOW/MEDIUM, not HIGH) due to dead-code status.

Every cited fact is accurate in C:\Users\amit0\desktop\fitness-app\src\components\workout\components\PerformanceAnalytics.tsx:
- Line 421: color="#FF9F0A" (זמן אימון); Line 426: color="#30D158" (סטים); Line 432: color="#BF5AF2" (RPE ממוצע) — raw hex StatCard colors bypassing tokens.
- Line 273: progress >= 1 ? '#30D158' : 'var(--fs-accent)' (MiniProgress color).
- Line 468: 'linear-gradient(90deg, #30D158, #34C759)' (overall-progress fill).
- Line 416: only "נפח כולל" correctly uses color="var(--fs-accent)".
- text-white/* and border-white/10 utilities are pervasive (lines 119,125,132,277,397,406,438,445,455,462, etc.).

This genuinely violates project conventions: DESIGN.md:17 declares a deliberate "two-accent system (mint action + lime signal)", and .claude/rules/common/design-aesthetics.md mandates "Never hardcode brand colors" + "Both modes" (light Fresh Steel / dark Obsidian). The hex set #FF9F0A/#30D158/#BF5AF2/#34C759 is an off-brand Apple palette (identical values duplicated in IntensityMeter.tsx:50-79,282), introducing 3 unowned accents and breaking dark-mode theming. tokens.css already provides the correct replacements (--fs-warn, --color-success, --fs-signal, --fs-accent-2). The proposed fix is appropriate.

CAVEAT that lowers the claimed HIGH severity: PerformanceAnalytics has zero callers (codegraph: "No callers found") and is not imported anywhere outside its own barrel (index.ts) — it is exported but never rendered in the live app. So there is no current user-facing breakage. Additionally, .premium-card has no CSS definition anywhere in the repo, so the card has no dark background of its own — the white-on-dark assumption already has no guaranteed dark surface. Per coding-style.md, pre-existing dead-code issues should be "mentioned, not deleted." Net: a real, accurate token/both-mode violation worth fixing if this component is ever revived, but not a HIGH-impact production bug today.

## [10] MEDIUM — ForecastChart uses raw success/error hex instead of semantic tokens (dark-mode blind)
**File:** src/components/workout/ForecastChart.tsx (line ~172)
**Dimension:** design-tokens
**Description:** Trend color uses hardcoded '#22c55e' (increasing) and '#ef4444' (decreasing). The design system already defines semantic success/error tokens that differ between modes: --color-success is #2f8f58 (light) / #34d98c (dark) and --color-error is #b83228 (light) / #ff5449 (dark). The raw Tailwind-green/red hex ignore both the brand values and the dark-mode brightening, so the trend indicator is off-palette and does not adapt for Obsidian.
**Proposed fix:** Use var(--color-success-fg) and var(--color-error-fg) (or var(--fs-accent)/var(--fs-warn) if a success/warn read is intended) for the increasing/decreasing branches; keep the existing var(--fs-muted) for the stable branch.
**Verifier evidence:** Verified against source. src/components/workout/ForecastChart.tsx lines 170-175 hardcode '#22c55e' (increasing) and '#ef4444' (decreasing) for the trend indicator, with var(--fs-muted) for stable. The cited semantic tokens exist and ARE mode-dependent in src/styles/tokens.css: light --color-success/-fg #2f8f58 and --color-error/-fg #b83228 (lines 69-76); dark (html.dark) --color-success/-fg #34d98c and --color-error/-fg #ff5449 (lines 429-436). The raw Tailwind green/red are static, so they neither match the brand values nor brighten for Obsidian dark mode — a genuine off-palette + dark-mode-blind defect. This directly violates the project's mandatory token rule in .claude/rules/common/design-aesthetics.md ('Never hardcode brand colors'; 'Both modes... must read correctly in light and dark'). Proposed fix (var(--color-success-fg)/var(--color-error-fg), keep --fs-muted for stable) is correct and matches existing component.css usage of these tokens for text color. Note: the same bug class recurs in src/services/bodyStatsService.ts (lines 193,195,390,393), so a broader sweep is warranted, but the finding's specific file/line and token claims are all accurate. MEDIUM severity is appropriate.

## [11] MEDIUM — Stale WORKOUT_THEMES preset map hardcodes brand + arbitrary hex in a 'theme system removed' stub
**File:** src/components/workout/themes.ts (line ~15)
**Dimension:** design-tokens
**Description:** The file header says 'STUB - Theme system removed' and getThemeVariables() returns {}, yet WORKOUT_THEMES still exports 5 presets full of hardcoded hex: brand values '#43C7A5'/'#2C7F91'/'#16292D'/'#B9C8C6' (duplicating --fs-accent/--fs-accent-2/--fs-primary/--fs-steel) plus arbitrary off-brand '#E26E3F','#D6453D','#fbbf24','#eab308'. This is both a token-discipline violation (brand hex must be var(--fs-*)) and dead/stale data that invites future misuse and drift from tokens.css.
**Proposed fix:** If the theme system is truly removed, delete WORKOUT_THEMES (and the now-unused ThemeVariable/THEME_PRESETS exports) after confirming no importers. If it must stay, replace the hardcoded brand hex with var(--fs-*) references and drop the non-brand colors.
**Verifier evidence:** VERIFIED REAL. File C:\Users\amit0\desktop\fitness-app\src\components\workout\themes.ts header says "STUB - Theme system removed" and getThemeVariables() returns {}, yet WORKOUT_THEMES (lines 15-41) exports 5 presets with hardcoded hex. The brand-hex duplication is confirmed against src/styles/tokens.css: #16292d=--fs-primary, #43c7a5=--fs-accent, #2c7f91=--fs-accent-2, #b9c8c6=--fs-steel (the file uses uppercase variants of these exact values), plus off-brand #E26E3F/#D6453D/#fbbf24/#eab308. This violates the project's token-discipline rule (.claude/rules/common/design-aesthetics.md: brand hex must be var(--fs-*)).\n\nIt is genuinely DEAD CODE: a src-wide grep for WORKOUT_THEMES, THEME_PRESETS, ThemeVariable, and getThemeVariables returns matches ONLY inside themes.ts itself — zero importers. No import of the themes module exists anywhere in src/. The plan doc plans/review-07-workout-components.md claim that SettingsPrimitives.tsx uses WORKOUT_THEMES is STALE/incorrect: SettingsPrimitives.tsx (at overlays/) has no theme references. So the proposed fix (delete the unused exports after confirming no importers) is safe and correct.\n\nSeverity nuance: the hardcoded hex lives in an unimported .ts data array that is never rendered, so it has NO runtime impact on dark mode/theming/contrast — unlike a hardcoded hex in a live component or CSS that the design rule primarily targets. It is stale dead data that duplicates token values and invites future drift. Real and worth fixing as cleanup, but effective severity is closer to LOW than the claimed MEDIUM.

## [12] MEDIUM — DESIGN.md is stale: documents a colored .fs-accent-rail that the CSS has already neutralized
**File:** src/styles/components.css (line ~998)
**Dimension:** design-tokens
**Description:** DESIGN.md and design-aesthetics.md treat .fs-accent-rail as a 4px colored accent bar that is 'over-applied' and must be reserved for one semantic role, listing it as a live anti-slop concern. But components.css line 993-1001 shows the colored side-stripe was already removed app-wide; the class now only does 'position: relative; overflow: hidden'. The class is still attached to ~14 surfaces (WorkoutStreak, MuscleDistribution, ConsistencyScore, WaterTracker, RecentPRBanner, ForecastNudge, CoachBriefCard, EmbeddedTemplatePicker, WorkoutCalendar, WorkoutHistory, SettingsCard, PreWorkoutScreen, StatsGrid, TrendChartCard). Result: a no-op decorative class littered across the codebase and a token-contract doc that contradicts the SSOT CSS, causing reviewer confusion.
**Proposed fix:** Reconcile the docs with reality: update DESIGN.md/design-aesthetics.md to reflect that the colored rail was retired, and either rename the now-containment-only class (e.g. .fs-card-contain) or remove it from surfaces that only relied on it for the (now gone) accent, keeping it only where overflow/position containment is actually needed.
**Verifier evidence:** The factual core is verified. components.css:998-1001 confirms .fs-accent-rail is now containment-only (position: relative; overflow: hidden) with a comment stating the colored 4px side-stripe "was removed app-wide." Meanwhile the docs still describe it as a live colored bar: DESIGN.md:81 calls it an "RTL-aware 4px accent bar... Currently over-applied — see audit," DESIGN.md:95 lists "One accent-rail role" as an anti-slop guardrail, and .claude/rules/common/design-aesthetics.md:38,50 still treat it as a colored "accent bar." That documentation genuinely contradicts the SSOT CSS, so doc-drift is REAL.

The drift is also self-contradicting: DESIGN.md:81 points readers to "the audit," but that audit (docs/anti-slop-audit-2026-06-02.md:12-24) marks this 🟢 RESOLVED / "not an issue" — the opposite of "over-applied." Worth a small fix to reconcile the two doc references.

HOWEVER, the finding overstates scope and its proposed fix partially conflicts with an explicit, documented project decision. The same audit (lines 21-23) deliberately keeps the class on the ~14 components and states it "must NOT be stripped... removing it would break their positioning/overflow containment." Verified consumers like SettingsCard.tsx (Card noPadding) and WorkoutStreak.tsx (flex card) rely on it for containment, not decoration. So the "no-op decorative class littered across the codebase" framing is inaccurate, and the rename/removal half of the proposed fix is contrary to convention. The genuinely worth-fixing part is narrow: update DESIGN.md:81/95 and design-aesthetics.md:38,50 to reflect that the colored rail was retired and the class is containment-only. That is a low-end MEDIUM doc reconciliation, not the broader cleanup proposed.

Relevant files (absolute): C:\Users\amit0\desktop\fitness-app\src\styles\components.css (998-1001), C:\Users\amit0\desktop\fitness-app\DESIGN.md (81,95), C:\Users\amit0\desktop\fitness-app\.claude\rules\common\design-aesthetics.md (38,50), C:\Users\amit0\desktop\fitness-app\docs\anti-slop-audit-2026-06-02.md (12-24).

