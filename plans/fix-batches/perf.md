## [28] HIGH — Supabase client (50KB gzip, largest chunk) is eagerly bundled and instantiated on first paint, blocking initial render
**File:** src/lib/supabase.ts (line ~15)
**Dimension:** perf-bundle
**Description:** src/lib/supabase.ts statically imports `createClient` from `@supabase/supabase-js` and calls it at module top-level (`export const supabase = isConfigured ? createClient(...) : null`). This module is imported by AuthContext.tsx (`import { isSupabaseConfigured, supabase } from '../lib/supabase'`), and AuthProvider is rendered eagerly from App.tsx (`<AuthProvider>` is NOT lazy). The built `supabase` chunk is the single largest in the app at 194656 bytes (≈50KB gzip) — larger than react-vendor and the main entry. Worse, AuthProvider starts in `status: 'loading'` (AuthContext.tsx line 49) and renders only `<PageLoader/>` until `supabase.auth.getSession()` resolves (lines 75-86), so the supabase chunk must both download and run before the app shell paints — even for guest/local-only users who configured no cloud and will never sync.
**Proposed fix:** Make supabase lazy: convert `src/lib/supabase.ts` to an async `getSupabase()` that dynamically `import('@supabase/supabase-js')` and memoizes the client, OR lazy-load AuthProvider's supabase-dependent path so the 50KB chunk is fetched after first paint. At minimum, do not gate the `loading` -> shell transition on a network `getSession()` round-trip; render the shell optimistically from the persisted session and reconcile async. Guests with `isSupabaseConfigured() === false` should never load this chunk at all.
**Verifier evidence:** VERIFIED against both source and the built bundle. Core perf-bundle claim is real and accurately measured. (1) src/lib/supabase.ts (lines 6, 15-22) statically imports createClient and instantiates the client at module top-level. (2) AuthContext.tsx line 18 eagerly imports it; AuthProvider is rendered eagerly from App.tsx line 172 (not lazy), and App gates the whole tree on status==='loading' returning <PageLoader/> (App.tsx lines 270-271). (3) Build output confirms dist/assets/supabase-CjiaG5jY.js = 194656 bytes — the single largest chunk, larger than react-vendor (172142) and the entry index-B2hrNwbD.js (190575). (4) Crucially, it is loaded EAGERLY, stronger than the finding states: index.html emits <link rel="modulepreload" ... supabase-CjiaG5jY.js>, AND the entry chunk contains a STATIC import (`import{c as y}from"./supabase-CjiaG5jY.js"`), not a dynamic import(). The vite manualChunks (vite.config.ts 124-126) only NAMES the chunk; it does not make it lazy. So ~50KB gzip sits on the critical path and parses before the React tree mounts, for every user including local-only/guests who never sync. The proposed fix (async getSupabase() with dynamic import + memoization, and never loading the chunk when isSupabaseConfigured()===false) is legitimate and worth doing.

CAVEATS / one inaccurate sub-claim in the DESCRIPTION (does not change the verdict for the perf-bundle dimension): the finding says the shell paint is gated on a network getSession() round-trip. That is wrong — supabase.auth.getSession() reads from localStorage with NO network call (explicitly documented at supabaseAuth.ts line 131; persistSession:true in supabase.ts), so the loading->shell transition waits only on a resolved-promise microtask, not a network hop. Also, for unconfigured/guest users AuthContext.tsx lines 66-70 resolve status synchronously WITHOUT calling supabase at all, so the 'even for guests, network-gated' framing misdescribes that path. But the bundling problem itself (largest chunk, eager static import + modulepreload, on the critical path, shipped to all users) is fully confirmed, so the finding is REAL and worth fixing.

## [29] HIGH — GSAP + Physics2DPlugin (28KB gzip) forced into the critical initial load via eagerly-imported BottomNav and Dashboard
**File:** src/components/ui/BottomNav.tsx (line ~17)
**Dimension:** perf-bundle
**Description:** BottomNav.tsx (line 17) statically imports `{ DUR, EASE, gsap, useGSAP } from '../../lib/gsap'`, and BottomNav is eagerly imported by App.tsx (line 23, `import BottomNav from './components/ui/BottomNav'`) — it renders in AppShell on every authenticated screen. src/lib/gsap.ts registers Physics2DPlugin at module scope (line 19). Additionally the first route, Dashboard.tsx, eagerly imports `useCountUp` (line 24) and `formatThousands` (line 26) from the same gsap lib, and ActivityRings.tsx (rendered on Dashboard) imports gsap (line 3). Net effect: the dedicated `gsap` chunk (72248 bytes / ≈28KB gzip per the build) is part of the initial app-shell + first-route load, even though GSAP only drives a decorative nav-pill slide and count-up animations that are non-essential to first paint and are skipped entirely under prefers-reduced-motion.
**Proposed fix:** Defer GSAP off the critical path. The nav-pill flow animation and count-ups are progressive enhancements: load `lib/gsap` via dynamic import inside the animation effect (after mount), falling back to a static final value when GSAP isn't loaded yet (the reduced-motion branch already snaps to final state, so the no-animation path exists). This keeps the 28KB gsap chunk out of the first-paint waterfall while preserving the animations a moment later.
**Verifier evidence:** VERIFIED REAL. Every claim checks out against the actual files AND the production build output.

Static import chain into the critical path (all confirmed):
- src/components/ui/BottomNav.tsx line 17: `import { DUR, EASE, gsap, useGSAP } from '../../lib/gsap'` (static).
- src/App.tsx line 23: `import BottomNav from './components/ui/BottomNav'` (static, eager). BottomNav renders in AppShell (line 615, `<MemoizedBottomNav />`) on every authenticated, non-/workout screen.
- src/lib/gsap.ts line 19: `gsap.registerPlugin(useGSAP, Physics2DPlugin)` at module scope, with Physics2DPlugin imported line 15 — so importing this SSOT module pulls the plugin into the chunk even though Physics2D is only used by celebration surfaces (gsapSparks.ts → WorkoutSummary/WaterTracker/PRCelebration/SlideToComplete), none on first paint.
- First route Dashboard.tsx imports useCountUp (line 24) and formatThousands (line 26) from the gsap lib; ActivityRings.tsx (charts, rendered on Dashboard) imports gsap (line 3).

Build/waterfall confirmation (the load-bearing evidence):
- vite.config.ts lines 130-131 route all gsap into a dedicated `gsap` chunk.
- dist/assets/gsap-CpBsHrzl.js is exactly 72248 bytes raw (matches the finding's figure; ~28KB gzip is a reasonable estimate).
- dist/index.html line 28 contains `<link rel="modulepreload" crossorigin href="/assets/gsap-CpBsHrzl.js">`. Vite only emits modulepreload for chunks statically reachable from the entry, proving the gsap chunk is in the initial critical-path load, not lazy.

The animations it drives are progressive enhancements: gsap.ts header comment, useCountUp.ts lines 60-63, and BottomNav line 183 all snap to the final state under reduced motion, so a no-animation fallback already exists — making the proposed deferred-dynamic-import fix feasible.

Severity nuance (does not change isReal): the chunk is modulepreloaded, not a render-blocking synchronous script, so it competes for bandwidth rather than hard-blocking first paint. That makes it a solid HIGH-leaning perf-bundle issue rather than a paint-blocker, but on a mobile Hebrew PWA pulling ~28KB gzip of purely decorative JS (nav-pill slide + count-ups) into the initial waterfall is genuinely worth fixing. Finding is accurate and actionable.

