## [2] HIGH — REMOVE_EXERCISE leaves dangling exercise IDs in supersetGroups
**File:** src/components/workout/core/workoutReducer.ts (line ~183)
**Dimension:** bugs-state
**Description:** REMOVE_EXERCISE only splices draft.exercises; it never touches draft.supersetGroups. A superset/giant-set that contains the removed exercise keeps the now-orphan id. Subsequent COMPLETE_SET superset logic (lines 324-362) iterates `supersetMembership.exercises` and calls `draft.exercises.find((e) => e.id === candidateId)` / `findIndex`; the orphan id resolves to `undefined`/`-1` so round detection silently skips it, and a 2-member superset can be left with a single real member (round logic then never advances). The UI's ExerciseReorder/SupersetPicker also render a phantom membership label for the missing id. Contrast with REMOVE_SUPERSET (line 254) and CREATE_SUPERSET (lines 240-242), which both explicitly prune group membership — REMOVE_EXERCISE should do the same.

Code: `case 'REMOVE_EXERCISE': { draft.exercises.splice(action.payload, 1); if (draft.currentExerciseIndex >= draft.exercises.length) { draft.currentExerciseIndex = Math.max(0, draft.exercises.length - 1); } break; }`
**Proposed fix:** After splicing, capture the removed exercise's id and prune it from every group, then drop degenerate groups: `const removed = draft.exercises[action.payload]; const removedId = removed?.id; draft.exercises.splice(action.payload, 1); if (removedId) { draft.supersetGroups = draft.supersetGroups.map((g) => ({ ...g, exercises: g.exercises.filter((id) => id !== removedId) })).filter((g) => g.exercises.length >= 2); }` (mirrors CREATE_SUPERSET's cleanup).
**Verifier evidence:** VERIFIED REAL. In C:\Users\amit0\desktop\fitness-app\src\components\workout\core\workoutReducer.ts the REMOVE_EXERCISE case (lines 183-189) only does `draft.exercises.splice(action.payload, 1)` plus a currentExerciseIndex clamp; it never touches `draft.supersetGroups`. So removing an exercise that belongs to a superset/giant-set leaves its id orphaned in the group. The dispatch site (src/components/workout/active/useWorkoutHandlers.ts line 292, handleRemoveExercise) just dispatches the action with no reconciliation, and no other handler prunes the group, so the reducer is the only place this could be fixed and it is not.\n\nThe downstream impact described is accurate. COMPLETE_SET's superset logic (lines 324-360) calls `draft.supersetGroups.find((g) => g.exercises.includes(exercise.id))`, then iterates `groupExercises` using `n = groupExercises.length` (still counting the orphan) and `draft.exercises.find((e) => e.id === candidateId)`. For the orphan id that find returns undefined and `if (!candidate) continue` (line 341) silently skips it. A 2-member superset reduced to one real member therefore finds no valid partner: targetExerciseId stays null, no transition fires, and the round/round-robin advance logic never triggers for the survivor.\n\nThe UI claim also holds: ExerciseReorder.tsx (lines 52-60) builds `supersetMembership` keyed by id with `total: group.exercises.length`, so a surviving member still renders a stale \"position X of N\" phantom label.\n\nThe contrast cited is correct: CREATE_SUPERSET (lines 240-242) and REMOVE_SUPERSET (line 256) both explicitly prune/drop degenerate groups; REMOVE_EXERCISE is the outlier. The proposed fix mirrors CREATE_SUPERSET's exact `draft.supersetGroups = draft.supersetGroups.map(...).filter((g) => g.exercises.length >= 2)` reassignment pattern, which is already proven valid in this Immer reducer, and reading removedId before splice is correct. No existing test covers REMOVE_EXERCISE vs supersetGroups (tests only cover CREATE/REMOVE_SUPERSET pruning), so the gap is untested. Severity is closer to MEDIUM-HIGH (requires the specific flow of removing a superset member mid-workout) rather than HIGH, but it is a genuine state-integrity bug worth fixing.

## [3] HIGH — REMOVE_EXERCISE keeps currentExerciseIndex pointing at the wrong exercise when deleting before the active one
**File:** src/components/workout/core/workoutReducer.ts (line ~185)
**Dimension:** bugs-state
**Description:** The only adjustment to currentExerciseIndex is a clamp when it goes out of bounds: `if (draft.currentExerciseIndex >= draft.exercises.length) { draft.currentExerciseIndex = Math.max(0, draft.exercises.length - 1); }`. Deleting an exercise at an index LOWER than currentExerciseIndex shifts every later exercise down by one, but the index is left unchanged, so the active exercise silently changes. Example: 3 exercises, currentExerciseIndex=1 (user mid-set on exercise B), user deletes exercise A (index 0) from the drawer/reorder sheet → after splice length=2, index 1 is still <2 so it is NOT adjusted, and it now points to what was exercise C instead of B. The card the user was working on swaps under them. Deletion is reachable from ExerciseReorder.handleDelete → onDeleteExercise → handleRemoveExercise → REMOVE_EXERCISE.
**Proposed fix:** Decrement the index when the removed position is at or before it: `const removedIdx = action.payload; draft.exercises.splice(removedIdx, 1); if (draft.currentExerciseIndex > removedIdx) { draft.currentExerciseIndex -= 1; } if (draft.currentExerciseIndex >= draft.exercises.length) { draft.currentExerciseIndex = Math.max(0, draft.exercises.length - 1); }`
**Verifier evidence:** Confirmed real and worth fixing (HIGH). In src/components/workout/core/workoutReducer.ts (REMOVE_EXERCISE, lines 183-189) the only adjustment to currentExerciseIndex is an out-of-bounds clamp: `draft.exercises.splice(action.payload, 1); if (draft.currentExerciseIndex >= draft.exercises.length) { draft.currentExerciseIndex = Math.max(0, draft.exercises.length - 1); }`. Deleting an exercise at an index LOWER than currentExerciseIndex shifts later exercises down but leaves the index unchanged, so it silently points at a different exercise. The active card is index-based, not id-based: WorkoutProvider.tsx:368 (`const currentExercise = state.exercises[state.currentExerciseIndex]`) and WorkoutContext.tsx:90 both derive the rendered exercise from `exercises[currentExerciseIndex]`. The deletion path is reachable with an arbitrary real array index: ExerciseReorder.tsx:128-130 computes `originalIdx = exercises.findIndex(ex => ex.id === exerciseToDelete.id)` and dispatches it via onDeleteExercise -> useWorkoutHandlers.ts:292 (`dispatch({ type: 'REMOVE_EXERCISE', payload: idx })`). Worked example: 3 exercises [A,B,C], currentExerciseIndex=1 (B); delete A (index 0); after splice length=2 and 1>=2 is false, so no adjustment, and index 1 now resolves to C — the card swaps under a user mid-set. No compensating logic exists. The proposed fix (decrement currentExerciseIndex when removedIdx < currentExerciseIndex, then keep the bounds clamp) is correct; the `>` comparison correctly leaves the active-exercise-deleted case to the clamp.

## [4] HIGH — REORDER_EXERCISES does not remap currentExerciseIndex, switching the active exercise after a reorder
**File:** src/components/workout/core/workoutReducer.ts (line ~191)
**Dimension:** bugs-state
**Description:** `case 'REORDER_EXERCISES': { draft.exercises = action.payload.filter((ex) => ex.name?.trim()); break; }` replaces the array but leaves currentExerciseIndex unchanged. After a drag-reorder the index now references a different exercise (positionally), so when the reorder sheet closes the live workout jumps to whatever exercise landed in the old slot. The `.filter((ex) => ex.name?.trim())` can also shorten the array (any temporarily-unnamed exercise is dropped), which can push currentExerciseIndex out of bounds with no clamp here — derived.currentExercise then becomes undefined and the UI falls back to the PreWorkoutScreen branch (ActiveWorkoutNew line 439) mid-workout. The reducer should preserve the active exercise by identity (id) across reorder.
**Proposed fix:** Capture the active exercise's id before replacing, then re-resolve its new position and clamp: `const activeId = draft.exercises[draft.currentExerciseIndex]?.id; draft.exercises = action.payload.filter((ex) => ex.name?.trim()); const newIdx = activeId ? draft.exercises.findIndex((e) => e.id === activeId) : -1; draft.currentExerciseIndex = newIdx !== -1 ? newIdx : Math.min(draft.currentExerciseIndex, Math.max(0, draft.exercises.length - 1));`
**Verifier evidence:** Verified end-to-end. workoutReducer.ts:191-194 REORDER_EXERCISES replaces draft.exercises but never remaps draft.currentExerciseIndex — confirmed verbatim. The index is purely positional: WorkoutProvider.tsx:368 derives currentExercise = state.exercises[state.currentExerciseIndex], so after a drag-reorder the index points at a different exercise. This is inconsistent with the sibling cases in the same slice that DO maintain the index — ADD_EXERCISE sets it (line 177) and REMOVE_EXERCISE clamps it (lines 185-187) — so the omission is a genuine gap, not an intentional convention. The dispatch path is a pure pass-through (useWorkoutHandlers.ts:275-280 dispatches the dragged order with no remap; ExerciseReorder.tsx:103 saves the user-dragged items), so nothing upstream compensates. The out-of-bounds → PreWorkoutScreen claim is also accurate: the .filter((ex) => ex.name?.trim()) can shorten the array with no clamp here, WorkoutProvider.tsx:370-372 returns currentExercise: undefined on a miss, and ActiveWorkoutNew.tsx:439 falls back to PreWorkoutScreen when !derived.currentExercise. No test (none reference REORDER_EXERCISES) or project rule sanctions the current behavior. The proposed fix (capture active id, re-resolve via findIndex, clamp) is correct and matches the codebase's existing id-stability pattern (RESTORE_DELETED_SET keys by exerciseId with index fallback, lines 570-597). Minor nuance on severity: the everyday symptom is the active exercise silently changing identity after reorder; the worst case (jump to PreWorkoutScreen mid-workout) needs a dropped unnamed exercise or a shortened array pushing the stale index out of bounds — narrower but real. HIGH severity is reasonable.

