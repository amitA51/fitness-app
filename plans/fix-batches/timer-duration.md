## [5] MEDIUM — Paused rest timer renders 00:00 and fires the rest-end ding because the local timer hook ignores the frozen (negative) endTime
**File:** src/components/workout/hooks/useWorkoutTimer.ts (line ~118)
**Dimension:** bugs-state
**Description:** TOGGLE_PAUSE freezes the rest timer by encoding the remaining time as a NEGATIVE endTime (`draft.restTimer.endTime = -remainingMs`, workoutReducer line 622). The reducer's SYNC_REST_TIMER respects this (line 681 bails while paused), but the local countdown hook `useRestTimer(endTime, active)` that actually drives InlineRestTimer does not know about pause: it computes `const left = Math.max(0, (endTime - now) / 1000)`. With a negative endTime this is always 0, so (a) the timer visually snaps to 00:00 while paused instead of holding the frozen remaining time, and (b) the `if (left <= 0 && !soundPlayedRef.current)` branch (line 118) immediately runs playDing() and dispatches SYNC_REST_TIMER on pause. `isPaused` is never threaded into WorkoutHeaderSection → InlineRestTimer → useRestTimer, so the hook cannot guard against it. NOTE: this is currently latent — no UI control dispatches the workout-level TOGGLE_PAUSE (the only TOGGLE_PAUSE in the tree, WarmupCooldownFlow.tsx:908, targets a separate local reducer) — but the freeze/restore machinery and the persisted `isPaused` restore path (WorkoutProvider lines 140-147) assume this works, so it is a real defect waiting on the pause button being wired.
**Proposed fix:** Pass isPaused down to InlineRestTimer/useRestTimer and treat a non-positive endTime as 'frozen': skip the countdown and the `left<=0` ding/dispatch when paused, deriving the displayed time from restTimer.timeLeft instead. E.g. in useRestTimer: `if (isPaused || (endTime !== null && endTime <= 0)) { setTimeLeft(Math.max(0, frozenRemaining)); return; }` before installing the interval, and gate the ding on `!isPaused`.
**Verifier evidence:** Verified all code-level claims and they hold exactly. (1) useRestTimer (src/components/workout/hooks/useWorkoutTimer.ts:108) computes `left = Math.max(0, (endTime - now) / 1000)`; with a negative endTime this is always 0, so the display snaps to 00:00 and the `if (left <= 0 && !soundPlayedRef.current)` branch at line 118 immediately calls playDing() and dispatches SYNC_REST_TIMER. (2) TOGGLE_PAUSE in workoutReducer.ts:621-622 does freeze the rest timer by encoding `endTime = -remainingMs`. (3) SYNC_REST_TIMER (line 681) correctly bails while paused/negative, but the local hook has no such guard. (4) isPaused is genuinely NOT threaded into InlineRestTimer: WorkoutHeaderSection.tsx receives isPaused (line 8/24/40) and passes it only to WorkoutHeader, while InlineRestTimer (lines 50-56) gets only `active` and `endTime`; InlineRestTimer.tsx has no isPaused prop and calls useRestTimer(endTime, active). So the hook cannot guard against pause.\n\nThe finding is also honest and correct that this is currently LATENT: the only dispatch({type:'TOGGLE_PAUSE'}) is WarmupCooldownFlow.tsx:908, which targets a separate LOCAL useReducer defined in that same file (line 810), NOT the workout-level reducer. I confirmed via full isPaused search that nothing else sets the workout-level isPaused=true (no visibilitychange auto-pause, no pause button in WorkoutHeader — only finish/overflow controls exist). Therefore restTimer.endTime is never negative in the current app and the bug cannot be triggered by a user today.\n\nVerdict: REAL, accurately diagnosed latent defect. The freeze/restore machinery (reducer negative-endTime encoding, SYNC_REST_TIMER guard, and the WorkoutProvider restore path at lines 140-147 that preserves persisted isPaused and the negative endTime) is all built assuming pause works, but the local countdown hook that actually drives the visible timer is inconsistent with it. The moment any pause control is wired to the workout-level reducer, this becomes a live bug (00:00 flash + spurious rest-end ding on pause). The proposed fix (thread isPaused down and treat non-positive endTime as frozen, gating the ding) is the correct surgical remedy. Worth fixing as a correctness gap in the pause subsystem; only caveat is that MEDIUM severity overstates current user-facing impact since it is presently unreachable.

## [6] MEDIUM — PR celebration misses records set inside a superset (PR effect reads the wrong exercise after auto-advance)
**File:** src/components/workout/hooks/usePersonalRecords.ts (line ~133)
**Dimension:** bugs-state
**Description:** The auto-PR effect only inspects the CURRENT exercise's last completed set: `const currentExercise = exercises[currentExerciseIndex]; ... const lastSet = completedSets[completedCount - 1]; ... checkForNewPR(name, lastSet)`. But during a superset, COMPLETE_SET reassigns draft.currentExerciseIndex to the next group member (workoutReducer lines 390-392) in the SAME dispatch that completes the set. By the time this effect runs, currentExerciseIndex already points at exercise B, so the set that was just completed on exercise A is never checked for a PR — the effect re-checks B's last completed set instead (and the lastPRCheckRef dedup is keyed on exerciseIdx, so A's set won't be revisited when the user returns). Supersets/giant-sets therefore silently swallow PRs.
**Proposed fix:** Detect newly-completed sets across ALL exercises rather than only `exercises[currentExerciseIndex]` — diff the completed sets against the previously-seen state per exercise id and run checkForNewPR for each newly-completed set. At minimum, drive the dedup ref by exercise id + set id so an auto-advance can't skip the just-finished exercise.
**Verifier evidence:** VERIFIED REAL. The auto-PR effect in src/components/workout/hooks/usePersonalRecords.ts (lines 133-170) inspects ONLY `exercises[currentExerciseIndex]`'s single last-completed set, then dispatches SHOW_PR_CELEBRATION. In src/components/workout/core/workoutReducer.ts COMPLETE_SET, the completed set belongs to `draft.exercises[draft.currentExerciseIndex]` (exercise A, line 267), but during a superset the reducer reassigns `draft.currentExerciseIndex = nextSupersetIdx` (exercise B) at line 391 within the SAME dispatch. So when the effect runs post-render, currentExerciseIndex already points at B; it checks B's last completed set, not the A-set that was just completed.\n\nThis is the ONLY runtime path that fires the in-workout PR celebration — ActiveWorkoutNew.tsx (line 246) only consumes getPRForExercise for display, and prService.checkForNewPR/batched checker are persistence/history paths, not the live celebration. So there is no backstop for the celebration moment.\n\nConcrete trace (superset A↔B, 2 sets each): completing A-set1 advances index to B (B has 0 completed → effect returns, A-set1 not checked); completing B-set1 advances to A (effect now checks A-set1 — delayed); completing A-set2 advances to B (effect checks B's stale last-completed B-set1, A-set2 NEVER checked); completing B-set2 finishes the group, index stays B (checks B-set2). A-set2 — the heaviest/last set of A per round, exactly where a PR is likely — is permanently dropped because the index never lands back on A with A-set2 as its last-completed set, and the dedup ref keyed on exerciseIdx (lines 148-159) doesn't help. No test covers the superset PR interaction (workoutReducer.test.ts SHOW_PR_CELEBRATION suite only tests single-exercise gating).\n\nSeverity MEDIUM is appropriate: silent loss of the live PR celebration for superset/giant-set users (UX/feature degradation, not data corruption or crash; the record itself may still be recomputed from history in the summary). The proposed fix (diff newly-completed sets across all exercises by exercise id, key dedup on exercise id + set id) is the correct remedy.

## [30] CRITICAL — UTC date-keying bug reintroduced for workout, measurement, recovery and check-in records
**File:** src/components/workout/components/WorkoutActions.tsx (line ~253)
**Dimension:** architecture
**Description:** dateUtils.ts explicitly created todayStr()/toLocalDateStr() to fix UTC mis-keying for users ahead of UTC (Israel UTC+2/+3), with a comment warning never to use `new Date().toISOString().slice(0,10)`. That exact anti-pattern is reintroduced at the data-write boundary in at least four user-data paths: WorkoutActions.tsx:253 sets the completed-session `date`; AddMeasurementModal.tsx:64 sets the body-measurement `date`; AddRecoveryModal.tsx:54 sets the recovery-log `date`; coach/checkInService.ts:67 defaults the check-in `date`. A workout finished or a measurement logged between local 00:00-03:00 is stored under the PREVIOUS calendar day. This corrupts the date key used for streaks, weekly grids, calendars, and per-day nutrition/body logs — silent data misattribution, not cosmetic.
**Proposed fix:** Import todayStr from utils/dateUtils and replace every `new Date().toISOString().slice(0,10)` data-key write with todayStr(). For arbitrary Date values use toLocalDateStr(date). (settingsService.ts:89 backup-filename use is acceptable; the four record-writes are the bug.)
**Verifier evidence:** CONFIRMED REAL and worth fixing. All four claimed write sites exist verbatim: WorkoutActions.tsx:253 (`date: new Date().toISOString().slice(0, 10)` on the completed WorkoutSession), AddMeasurementModal.tsx:64 (body-measurement date), AddRecoveryModal.tsx:54 (recovery-log date), and coach/checkInService.ts:67 (check-in date default). The project's src/utils/dateUtils.ts deliberately defines todayStr() (line 72) and toLocalDateStr() (line 82) for exactly this purpose, with an explicit doc-comment (lines 62-83) warning that `new Date().toISOString().split('T')[0]` / `.slice(0,10)` mis-keys early-morning entries (local 00:00-03:00) to the previous calendar day for users ahead of UTC such as Israel (UTC+2/+3), and that local date keeps date-keyed logs on the correct day. The anti-pattern is reintroduced at these data-write boundaries, so a workout/measurement/recovery/check-in logged between local midnight and 03:00 is stored under the previous day's key. This corruption is observable: consumers like dateUtils isToday()/fmtDate()/getWeekStart() use LOCAL date components, while WorkoutCalendar.tsx:147 uses the UTC slice — so the stored UTC-skewed date key is inconsistent across consumers, producing silent misattribution in streaks, weekly grids and calendars, not a cosmetic issue. The finding correctly excludes settingsService.ts:89 (a backup-filename timestamp, not a data key). The proposed fix (import todayStr/toLocalDateStr and replace the four record-writes) is the correct, low-risk remediation. Note: the finding's scope is slightly understated — WorkoutCalendar.tsx:147 and reminderService.ts:88 also use the UTC slice — but that does not detract from the four cited bugs being real.

