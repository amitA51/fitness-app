## [21] HIGH — ExerciseReorder bottom sheet has no dialog role, focus trap, or Escape-to-close
**File:** src/components/workout/ExerciseReorder.tsx (line ~198)
**Dimension:** a11y
**Description:** This bottom sheet is rendered via createPortal(sheetContent, document.body) with a fixed full-screen backdrop (`position: fixed; inset: 0; zIndex: 9998`) and a sheet at zIndex 9999, but unlike the app's canonical ModalOverlay/Sheet it has NO `role="dialog"`, NO `aria-modal="true"`, NO focus trap, and NO Escape key handler. The only ways to dismiss are a backdrop click or a downward drag-fling (handleDragEnd at line 165). Keyboard-only and screen-reader users cannot perceive it as a modal nor close it with Escape, and Tab focus leaks to the page behind the backdrop. Compare to ModalOverlay.tsx which wires role/aria-modal + useFocusTrap + closeOnEscape correctly. WCAG 2.2 2.1.2 (No Keyboard Trap)/4.1.2 (Name, Role, Value).
**Proposed fix:** Render this sheet through the shared `<Sheet>` / `<ModalOverlay variant="bottomSheet">` wrapper (which provides role=dialog, aria-modal, useFocusTrap, scroll lock, and Esc-to-close), or at minimum add `role="dialog" aria-modal="true" aria-label="סדר תרגילים"` to the sheet element, call useFocusTrap(sheetRef, { isOpen: true, onClose, closeOnEscape: true }), and add a keydown Escape handler.
**Verifier evidence:** VERIFIED REAL. In C:\Users\amit0\desktop\fitness-app\src\components\workout\ExerciseReorder.tsx the bottom sheet is rendered via createPortal(sheetContent, document.body) (line 489) with a fixed full-screen backdrop at zIndex 9998 (lines 208-214) and a sheet at zIndex 9999 (lines 218-241). The sheet element has NO role="dialog", NO aria-modal="true", NO aria-label, NO focus trap (no useFocusTrap import/call anywhere in the file), and NO Escape/keydown handler. The only dismissal paths are backdrop onClick (line 207) and a downward drag-fling in handleDragEnd (lines 165-170). The comparison in the finding is accurate: the canonical src/components/ui/ModalOverlay.tsx wires role="dialog" + aria-modal="true" + aria-label (lines 219-222) and useFocusTrap(contentRef, { closeOnEscape }) (line 135), and src/hooks/useFocusTrap.ts implements Escape-to-close (closeOnEscape default true). The proposed-fix wrapper exists too: src/components/ui/Sheet.tsx is exactly <ModalOverlay variant="bottomSheet"> providing role/aria-modal, focus trap, scroll lock, and Esc-to-close. The component is LIVE, not dead code: it is lazy-loaded and mounted in src/components/workout/active/WorkoutFlowOverlays.tsx (line 22) as the active-workout exercise drawer, gated by isDrawerOpen. This is a genuine WCAG 2.2 2.1.2 / 4.1.2 gap on a primary interactive surface, and the repo already ships the exact reusable primitives to fix it. Minor caveat: line refs are approximate (sheet m.div at 218, not 198) but substance is precise. HIGH severity is justified.

## [22] HIGH — Drag-to-reorder handle is a div with only onPointerDown — no keyboard reorder path
**File:** src/components/workout/reorder/ExerciseReorderItem.tsx (line ~93)
**Dimension:** a11y
**Description:** The reorder grip is `<div onPointerDown={(e)=>{e.stopPropagation(); dragControls.start(e);}} style={{cursor:'grab'}}>` with no `tabIndex`, no `role`, no `aria-label`, and no key handlers. The parent Reorder.Item uses `dragListener={false}` so reordering is driven solely by this pointer-only handle. There is no keyboard mechanism (e.g. arrow keys / move-up-move-down buttons) to reorder exercises, so keyboard and switch-access users cannot reorder at all. WCAG 2.2 2.1.1 (Keyboard) and 2.5.7 (Dragging Movements).
**Proposed fix:** Add keyboard reordering: make the handle a real `<button type="button" aria-label="גרור לשינוי סדר">` with onKeyDown for ArrowUp/ArrowDown that swaps items in the list (mirroring handleReorder), or add explicit up/down move buttons per row. Ensure the control is focusable (tabIndex 0) and announces position (e.g. aria-label `"תרגיל {n} מתוך {total}"`).
**Verifier evidence:** VERIFIED REAL. In C:\Users\amit0\desktop\fitness-app\src\components\workout\reorder\ExerciseReorderItem.tsx, the drag handle (lines 93-108) is exactly as described: a bare `<div onPointerDown={(e)=>{e.stopPropagation(); dragControls.start(e);}} style={{cursor:'grab', ...}}>` wrapping a GripVertical icon — no tabIndex, no role, no aria-label, no key handlers. The parent Reorder.Item sets `dragListener={false}` (line 63), so reordering can ONLY be initiated via this pointer handle. I also checked the parent (src/components/workout/ExerciseReorder.tsx): it uses framer-motion `Reorder.Group`/`Reorder.Item` with `onReorder={handleReorder}` (lines 95-97, 419-450) and provides NO up/down move buttons and NO arrow-key handlers anywhere. framer-motion's Reorder has no built-in keyboard support, so keyboard-only and switch-access users genuinely cannot reorder exercises — a legitimate WCAG 2.2 2.1.1 (Keyboard) failure, with 2.5.7 (Dragging Movements) also relevant since dragging is the sole path. HIGH severity is justified: a core interaction (reordering the workout) is fully inaccessible without a pointer. Worth fixing. Note on the proposed fix: turning the grip into a button with ArrowUp/ArrowDown works, but explicit per-row up/down move buttons (calling handleReorder/setItems) are the more robust pattern since framer-motion Reorder offers no native keyboard reordering; the grip also still needs an aria-label regardless.

## [23] HIGH — CreateTemplateModal lacks dialog role, aria-modal, and focus trap
**File:** src/pages/templates/components/CreateTemplateModal.tsx (line ~110)
**Dimension:** a11y
**Description:** The outer overlay is `<m.div className="fixed inset-0 z-50 ... bg-black/80" onClick={onClose}>` with the sheet inside it. It adds a manual window keydown Escape handler (good) but has NO `role="dialog"`, NO `aria-modal="true"`, and NO focus trap. Tab order can move into the page behind the sheet, and screen readers don't announce a dialog context. The nested exercise picker (showExercisePicker, line 344) is also untrapped. The title `<h2>תבנית חדשה` is not referenced via aria-labelledby. WCAG 2.2 4.1.2 and 2.4.3 (Focus Order).
**Proposed fix:** Wrap the form in the shared `<Sheet>` (bottomSheet ModalOverlay) which supplies role=dialog, aria-modal, focus trap, scroll lock, and Esc. If kept custom, add `role="dialog" aria-modal="true" aria-labelledby={titleId}` to the inner sheet, attach useFocusTrap to it, and give the h2 `id={titleId}`.
**Verifier evidence:** Verified against the actual file. CreateTemplateModal.tsx renders a custom overlay (m.div lines 110-126) with NO role="dialog", NO aria-modal="true", and NO focus trap — it only has a manual window keydown Escape handler (lines 39-51). Tab can escape into the page behind the sheet; the h2 "תבנית חדשה" (lines 142-152) has no id and is not referenced via aria-labelledby/describedby. The nested exercise picker (lines 343-453) is also untrapped. This is a genuine WCAG 2.2 4.1.2 + 2.4.3 gap, not intentional: the project has a canonical Sheet component (src/components/ui/Sheet.tsx) built on ModalOverlay that already supplies role=dialog, aria-modal, focus trap (via src/hooks/useFocusTrap.ts), scroll lock, Esc, and title association — this modal is an outlier that bypasses the established pattern. The proposed fix (migrate to <Sheet>) is correct and matches codebase conventions. Minor imprecision: the fix says aria-labelledby, but the shared Sheet actually associates the title via ariaDescribedBy/ariaLabel; substance is unaffected. Severity HIGH is justified for a keyboard/screen-reader modal barrier.

