## [31] HIGH — WorkoutDetail.tsx (1173 lines) reimplements canonical set/volume/reps math instead of computeSessionStats
**File:** src/pages/WorkoutDetail.tsx (line ~50)
**Dimension:** architecture
**Description:** The page hand-rolls calculateTotalSets (l.50), calculateTotalReps (l.54), getBestSet (l.63), plus inline `ex.sets.filter(s=>s.isCompleted).reduce(...setVolume)` at l.429 and l.643, duplicating logic that workoutMath.ts already centralizes (computeSessionStats returns totalSets/totalReps/exerciseStats/bestSet; completedSetsVolume covers the volume reduce). WorkoutSummary.tsx and PerformanceAnalytics.tsx already delegate to computeSessionStats; WorkoutDetail and WorkoutHistory.tsx:125 do not. This is exactly the drift the SSOT was built to prevent — e.g. WorkoutDetail's getBestSet counts warmups while computeSessionStats can exclude them, so 'best set' on the detail page can disagree with the summary.
**Proposed fix:** Replace the four local helpers + the two inline reduces with computeSessionStats(session, {...}) / completedSetsVolume(ex.sets), matching the options WorkoutSummary uses. Removes ~40 lines and aligns the numbers across screens.
**Verifier evidence:** VERIFIED REAL (severity overstated). The SSOT exists and is deliberate: src/utils/workoutMath.ts is documented as the single source of truth so "the three screens cannot drift apart" and exposes computeSessionStats (totalVolume/totalSets/totalReps/exerciseStats with bestSet) and completedSetsVolume. WorkoutDetail.tsx genuinely re-implements this: calculateTotalSets (l.50), calculateTotalReps (l.54), getBestSet (l.63), plus inline completed-set volume reduces at l.147 (ExerciseCard) and l.429 (MuscleBreakdown). Every other consumer already delegates to the SSOT (WorkoutSummary l.56, PerformanceAnalytics l.88, analyticsService, progressionService, trainingLoadService, insightsAggregator, contextBuilder), so WorkoutDetail is a true outlier — exactly the drift the SSOT was built to prevent.

Two inaccuracies in the finding, neither fatal: (1) the cited second inline reduce at l.643 is wrong — l.643 is `ex.sets.filter(s=>s.isCompleted).length`, a set count, not a setVolume reduce; the real second volume reduce is at l.147. (2) The "getBestSet counts warmups / can disagree with summary" claim is only an edge case: getBestSet ranks by setVolume(), which returns 0 for warmups, so a warmup can only become "best" when no real working set has positive volume — not a divergence in normal sessions.

Why severity is MEDIUM not HIGH: the practical numbers do NOT currently disagree. completedSetsVolume reads `isCompleted` (matching WorkoutDetail's WorkoutSet shape) so the volume reduces are a clean swap; computeSessionStats reads `completed`/`completedAt` (not `isCompleted`), but the reducer (workoutReducer.ts l.309-310, 480-481) always sets completedAt and isCompleted together, so they stay in sync for persisted sessions. So the duplication is a latent-drift / maintainability issue (DRY violation against the project's own explicit SSOT convention) rather than an active wrong-number bug.

WORTH FIXING: yes — consolidating onto computeSessionStats(session,{excludeWarmup:...})/completedSetsVolume aligns WorkoutDetail with every sibling screen, removes ~40 lines, and closes the warmup edge-case divergence. The proposed fix is directionally correct; an implementer must (a) use the right line refs (147/429, not 643) and (b) match the completion-field shape (computeSessionStats expects completedAt/completed, which persisted sessions carry).

## [33] MEDIUM — WorkoutDetail default component is a ~590-line do-everything component (fetch + derive + present)
**File:** src/pages/WorkoutDetail.tsx (line ~580)
**Dimension:** architecture
**Description:** `export default function WorkoutDetail()` spans l.580-1173. It owns data fetching (useEffect + Promise.all of getWorkoutSession/getWorkoutSessions), derived analytics (derivePreviousSession, comparison math), share-text assembly (handleShare l.632-669 builds Hebrew share string inline), AND the full presentation tree, on top of 5 sibling presentational components and 3 stat helpers in the same file (1173 lines total — well over the 800 cap in coding-style.md). This mixes the three concerns the rules call out and makes the page hard to test in isolation.
**Proposed fix:** Extract data loading into a useWorkoutDetail(id) hook (returns {session, previousSession, loading, error}), move derivePreviousSession + share-text builder to a workoutDetail helpers module, and split ExerciseCard/MuscleBreakdown/StatItem into sibling files. Target <400 lines for the page.
**Verifier evidence:** Verified against C:\Users\amit0\desktop\fitness-app\src\pages\WorkoutDetail.tsx. Facts check out: file is exactly 1173 lines (over the 800 "max" stated in .claude/rules/common/coding-style.md and code-review.md). The default WorkoutDetail() component runs lines 580-1173 (~594 lines) and genuinely mixes three concerns: data fetching (useEffect + Promise.all of getWorkoutSession/getWorkoutSessions, l.590-630), derived analytics (derivePreviousSession at l.615 + inline comparison math at l.1049-1055), and the full presentation tree (l.749-1172). handleShare (l.632-669) builds the Hebrew share string inline. Sibling components (DetailSkeleton l.84, ExerciseCard l.144, StatItem l.325, MuscleBreakdown l.424) and stat helpers (calculateTotalSets l.50, calculateTotalReps l.54, getBestSet l.63) all share the file. This is an outlier, not a tolerated convention: every other page file is 143-616 lines (next-largest Dashboard at 616), and the project's own coding-style.md explicitly mandates "many small files > few large files" and "Files are focused (<800 lines)". No correctness bug exists and the code is internally well-organized, so this is a MEDIUM maintainability/testability issue rather than a defect; the proposed extraction (useWorkoutDetail hook + helpers module + sibling component files) is a low-risk refactor that aligns with the documented standard. Real and worth fixing.

